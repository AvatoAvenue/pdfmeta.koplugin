local _ = require("gettext")
return {
    name = "pdfmeta",
    fullname = _("PDF Calibre Metadata"),
    description = _([[Extracts Calibre metadata (series, series index, language) from PDF files.]]),
}